{% extends 'base.html.twig' %}

{% block body %}
<main class="flex flex-col items-center justify-center h-screen w-full bg-gray-100">
    <h1 class="text-black text-2xl title-font font-bold mb-2">Connexion</h1>

    {{ form_start(formLogin) }}
    {{ form_widget(formLogin) }}
    <button type="submit" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">Se connecter</button>
    {{ form_end(formLogin) }}

</main>

{% endblock %}